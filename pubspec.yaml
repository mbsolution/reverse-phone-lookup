name: reverse_lookup_flutter_app
description: A reverse phone lookup app with social integration.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0
  url_launcher: ^6.2.4

flutter:
  uses-material-design: true
